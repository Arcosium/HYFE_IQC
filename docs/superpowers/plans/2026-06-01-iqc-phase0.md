# IQC Phase 0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** HYFE_IQC 캐시 정확성 버그(settings-blind) 제거 + lint drop-only 보완(자동수정) + 흩어진 연산자 리스트 단일 카탈로그화 — 전부 브라우저 자동화 유지, API 불사용.

**Architecture:** 순수 모듈 3개 신규(`settings_fp`/`operator_catalog`/`alpha_repair`) + db 마이그레이션(v1→v2, settings 타입 컬럼)으로 캐시 키를 `(code_hash, settings_fingerprint)` 로 확장하고, worker/gemini 통합부에서 fp 매칭·자동수정·카탈로그 소비로 교체.

**Tech Stack:** Python 3.9(서버)/3.11(테스트·playwright), SQLite(`PRAGMA user_version` 게이트), pytest. 테스트는 **반드시 `python3.11 -m pytest`** (기본 python 은 argon2 import 에서 죽음).

> **커밋 정책:** 이 저장소는 "사장 명시 요청 시에만 커밋". 각 Task 끝의 **검증** 스텝은 테스트 실행이며, git commit 은 사장 승인 시 일괄. (외부 auto-backup 도구가 스냅샷 커밋을 별도로 함.)
> **반영:** 모듈 변경이므로 완료 후 `sudo systemctl restart hyfe-iqc.service` 필요.

---

## 파일 구조

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `server/settings_fp.py` | effective settings(기본값 채움) + fingerprint. 순수, 무의존. | 신규 |
| `server/operator_catalog.py` | brain_operators.csv → 단일 연산자 카탈로그(이름집합 + ts_/vec_ 휴리스틱). | 신규 |
| `server/alpha_repair.py` | 시뮬 전 결정적 자동수정(region-prefix·doubled-op·선행연산자·missing-lookback). | 신규 |
| `server/db.py` | 마이그레이션 v2(settings 컬럼+fp+self_corr+index), insert_alpha, lookup_alpha_by_hash, operator_preference_stats. | 수정 |
| `server/result_cache.py` | lookup(+settings_fp). | 수정 |
| `server/worker.py` | 캐시 조회(fp 매칭) + insert(settings/delay 전달). | 수정 |
| `server/gemini_strategist.py` | _filter_by_lint(+forced_delay+repair) + 호출부 2곳. | 수정 |
| `server/alpha_similarity.py` | _KNOWN_OPS → operator_catalog. | 수정 |
| `tests/test_settings_fp.py` 등 | 단위 테스트. | 신규 |

---

## Task 1: settings_fp 모듈 (effective settings + fingerprint)

**Files:**
- Create: `server/settings_fp.py`
- Test: `tests/test_settings_fp.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_settings_fp.py`:
```python
from server import settings_fp as sf


def test_default_equivalence():
    # 부분 dict 와 기본값-동치 dict 는 같은 fingerprint.
    a = sf.settings_fingerprint(sf.effective_settings({'universe': 'TOP3000'}, '1'))
    b = sf.settings_fingerprint(sf.effective_settings({}, '1'))
    assert a == b


def test_numeric_normalization():
    a = sf.settings_fingerprint(sf.effective_settings({'truncation': '0.010', 'decay': '4.0'}, '1'))
    b = sf.settings_fingerprint(sf.effective_settings({'truncation': '0.01', 'decay': '4'}, '1'))
    assert a == b


def test_case_normalization():
    a = sf.settings_fingerprint(sf.effective_settings({'universe': 'top3000'}, '1'))
    b = sf.settings_fingerprint(sf.effective_settings({'universe': 'TOP3000'}, '1'))
    assert a == b


def test_delay_injected_and_partial_delay_ignored():
    # forced_delay 가 fp 를 좌우하고, partial 의 delay 는 무시된다.
    d0 = sf.settings_fingerprint(sf.effective_settings({'delay': '1'}, '0'))
    d1 = sf.settings_fingerprint(sf.effective_settings({'delay': '0'}, '1'))
    assert d0 != d1
    assert sf.effective_settings({'delay': '1'}, '0')['delay'] == '0'


def test_different_settings_differ():
    a = sf.settings_fingerprint(sf.effective_settings({'neutralization': 'SECTOR'}, '1'))
    b = sf.settings_fingerprint(sf.effective_settings({'neutralization': 'INDUSTRY'}, '1'))
    assert a != b


def test_deterministic():
    eff = sf.effective_settings({'universe': 'TOP500'}, '1')
    assert sf.settings_fingerprint(eff) == sf.settings_fingerprint(eff)
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest tests/test_settings_fp.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'server.settings_fp'`

- [ ] **Step 3: 구현**

`server/settings_fp.py`:
```python
"""알파 시뮬 settings 의 정규화 + fingerprint.

캐시 키를 (code_hash, settings_fingerprint) 로 만들기 위한 순수 모듈.
'요청 settings' 가 아니라 '실제 적용될 effective settings'(WQB 기본값 채움) 위에서
계산하므로 {universe:TOP3000} 와 {} 가 같은 fingerprint 를 갖는다.
delay 는 라운드의 forced_delay 에서 주입한다 (Gemini settings 의 delay 는 무시).
"""
from __future__ import annotations

import hashlib

# Gemini 가 키를 생략하면 이 값으로 시뮬된다 (WQB UI 기본값).
WQB_DEFAULTS = {
    'region': 'USA',
    'universe': 'TOP3000',
    'neutralization': 'INDUSTRY',
    'decay': '0',
    'truncation': '0.01',
    'pasteurization': 'ON',
    'nan_handling': 'OFF',
}
# fingerprint 직렬화 순서 (고정).
_FP_KEYS = ('region', 'universe', 'delay', 'neutralization', 'decay',
            'truncation', 'pasteurization', 'nan_handling')


def _norm_val(v) -> str:
    """숫자는 표기 통일(0.010->0.01, 4.0->4), 그 외 문자열은 대문자화."""
    s = str(v).strip()
    try:
        f = float(s)
    except (TypeError, ValueError):
        return s.upper()
    if f == int(f):
        return str(int(f))
    return f'{f:.6f}'.rstrip('0').rstrip('.')


def effective_settings(partial, forced_delay) -> dict:
    """WQB_DEFAULTS 위에 partial 을 덮고 delay 는 forced_delay 로 강제. 값 정규화."""
    eff = dict(WQB_DEFAULTS)
    if isinstance(partial, dict):
        for k, v in partial.items():
            kk = str(k).strip().lower()
            if kk in WQB_DEFAULTS and v is not None and str(v).strip():
                eff[kk] = str(v).strip()
    eff['delay'] = str(forced_delay)
    return {k: _norm_val(val) for k, val in eff.items()}


def settings_fingerprint(eff: dict) -> str:
    raw = '|'.join(f'{k}={eff.get(k, "")}' for k in _FP_KEYS)
    return hashlib.sha256(raw.encode('utf-8')).hexdigest()[:16]
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest tests/test_settings_fp.py -v`
Expected: PASS (6 passed)

- [ ] **Step 5: 검증(체크포인트)** — 위 PASS 확인. (git commit 은 사장 승인 시.)

---

## Task 2: operator_catalog 모듈

**Files:**
- Create: `server/operator_catalog.py`
- Test: `tests/test_operator_catalog.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_operator_catalog.py`:
```python
from server import operator_catalog as oc


def test_seed_ops_always_recognized():
    # CSV 유무와 무관하게 기존 _KNOWN_OPS 핵심은 연산자로 인식 (무회귀 보장).
    for op in ('rank', 'ts_mean', 'add', 'group_neutralize', 'trade_when'):
        assert oc.is_operator(op), op


def test_ts_prefix_needs_lookback():
    assert oc.needs_lookback('ts_mean')
    assert oc.needs_lookback('ts_rank')
    assert not oc.needs_lookback('rank')
    assert not oc.needs_lookback('add')


def test_unknown_token_not_operator():
    assert not oc.is_operator('close')
    assert not oc.is_operator('anl4_some_field')


def test_operator_names_superset_of_seed():
    names = oc.operator_names()
    assert {'rank', 'add', 'ts_delta'} <= names


def test_case_insensitive():
    assert oc.is_operator('TS_MEAN')
    assert oc.needs_lookback('TS_MEAN')
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest tests/test_operator_catalog.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 구현**

`server/operator_catalog.py`:
```python
"""brain_operators.csv 기반 단일 연산자 카탈로그.

3곳에 흩어진 하드코딩 op 리스트(alpha_similarity._KNOWN_OPS, db.operator_preference_stats
KNOWN_OPS)를 대체한다. arity 는 advisory (CSV description 이 짧아 hard-gate 안 함).
주용도: 연산자 이름 집합(Jaccard op/field 분류) + ts_*/vec_* 접두 휴리스틱.
무회귀 보장: CSV 파싱 결과를 내장 시드와 합집합한다 (이전보다 적게 인식하지 않음).
CSV 부재/파싱 실패 시 시드만으로 동작.
"""
from __future__ import annotations

import csv
import os

_THIS_DIR = os.path.dirname(os.path.abspath(__file__))
OPERATORS_CSV = os.path.join(_THIS_DIR, 'brain_operators.csv')

# 구 _KNOWN_OPS 합집합 — 무회귀 baseline.
_SEED_OPS = frozenset({
    'rank', 'ts_rank', 'ts_delta', 'ts_mean', 'ts_std_dev', 'ts_sum', 'ts_min', 'ts_max',
    'ts_zscore', 'ts_corr', 'ts_decay_linear', 'ts_arg_min', 'ts_arg_max',
    'winsorize', 'zscore', 'delta', 'correlation', 'add', 'subtract', 'multiply',
    'divide', 'power', 'signed_power', 'abs', 'log', 'sqrt', 'inverse', 'min', 'max',
    'sign', 'reverse', 'group_rank', 'group_neutralize', 'group_sum', 'group_mean',
    'sum', 'mean', 'std_dev', 'scale', 'normalize', 'fraction', 'quantile',
    'if_else', 'trade_when', 'pasteurize', 'truncate', 'last_diff_value',
    'and', 'or', 'not', 'true', 'false', 'filter',
})

_CACHE = None  # (mtime_or_None, dict[name -> meta])


def _meta_for(name: str) -> dict:
    return {
        'category': '',
        'desc': '',
        'needs_lookback': name.startswith('ts_'),
        'scope': 'VECTOR' if name.startswith('vec_') else 'REGULAR',
    }


def _load() -> dict:
    global _CACHE
    try:
        mt = os.path.getmtime(OPERATORS_CSV)
    except OSError:
        mt = None
    if _CACHE is not None and _CACHE[0] == mt:
        return _CACHE[1]

    ops = {n: _meta_for(n) for n in _SEED_OPS}   # baseline
    if mt is not None:
        try:
            with open(OPERATORS_CSV, 'r', encoding='utf-8') as f:
                for row in csv.DictReader(f):
                    name = (row.get('name') or '').strip().lower()
                    if not name:
                        continue
                    m = ops.get(name) or _meta_for(name)
                    m['category'] = (row.get('category') or '').strip()
                    m['desc'] = (row.get('description') or '').strip()
                    ops[name] = m
        except Exception:
            pass
    _CACHE = (mt, ops)
    return ops


def operator_names() -> frozenset:
    return frozenset(_load().keys())


def is_operator(tok) -> bool:
    return (str(tok or '').strip().lower()) in _load()


def needs_lookback(name) -> bool:
    m = _load().get(str(name or '').strip().lower())
    return bool(m and m['needs_lookback'])
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest tests/test_operator_catalog.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: 검증(체크포인트)** — PASS 확인.

---

## Task 3: alpha_repair 모듈

**Files:**
- Create: `server/alpha_repair.py`
- Test: `tests/test_alpha_repair.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_alpha_repair.py`:
```python
from server import alpha_repair as ar


def test_region_prefix_strip():
    out, applied = ar.repair('rank(USA.close)', delay='1')
    assert out == 'rank(close)'
    assert 'region_prefix' in applied


def test_doubled_operator_collapse():
    out, applied = ar.repair('rankrank(close)', delay='1')
    assert out == 'rank(close)'
    assert 'doubled_op' in applied


def test_leading_operator_strip():
    out, applied = ar.repair('+ts_mean(close,5)', delay='1')
    assert out == 'ts_mean(close,5)'
    assert 'leading_op' in applied


def test_missing_lookback_delay0():
    out, applied = ar.repair('ts_mean(close)', delay='0')
    assert out == 'ts_mean(close,22)'
    assert 'missing_lookback' in applied


def test_missing_lookback_delay1():
    out, _ = ar.repair('ts_mean(close)', delay='1')
    assert out == 'ts_mean(close,10)'


def test_missing_lookback_respects_nested_arg():
    # 단일 인자가 중첩 함수(내부 콤마는 depth>1)면 윈도우를 붙인다.
    out, _ = ar.repair('ts_mean(add(close,open))', delay='1')
    assert out == 'ts_mean(add(close,open),10)'


def test_no_lookback_when_window_present():
    out, applied = ar.repair('ts_mean(close,5)', delay='1')
    assert out == 'ts_mean(close,5)'
    assert 'missing_lookback' not in applied


def test_non_ts_operator_untouched():
    out, applied = ar.repair('rank(close)', delay='0')
    assert out == 'rank(close)'
    assert applied == []


def test_idempotent():
    once, _ = ar.repair('+ts_mean(USA.close)', delay='0')
    twice, applied2 = ar.repair(once, delay='0')
    assert once == twice
    assert applied2 == []
```

- [ ] **Step 2: 실패 확인**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest tests/test_alpha_repair.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 구현**

`server/alpha_repair.py`:
```python
"""시뮬 전 결정적 자동수정 pre-pass.

lint 의 drop-only 정책을 보완 — 사소한 기계적 결함을 1회 수정해 구조적으로 새로운
아이디어를 typo 하나로 버리지 않는다. 의미 보존: 명시된 안전 케이스만 수정하고
모호하면 손대지 않는다 (멱등).
"""
from __future__ import annotations

import re

from . import operator_catalog

# delay-aware missing-lookback 기본 윈도우 (delay0 은 턴오버 억제 위해 큰 값).
LOOKBACK_DELAY0 = 22
LOOKBACK_DELAY1 = 10

_REGION_PREFIX_RX = re.compile(
    r'\b(?:USA|EUR|EUROPE|CHN|CHINA|ASI|AMR|GLB|JPN|KOR|TWN|HKG)\.([A-Za-z_]\w*)')
_LEADING_OP_RX = re.compile(r'^\s*[+*]\s*')
_OP_CALL_RX = re.compile(r'\b([a-z_][a-z0-9_]*)\s*\(')


def _strip_region_prefix(code: str) -> str:
    return _REGION_PREFIX_RX.sub(r'\1', code)


def _collapse_doubled_ops(code: str) -> str:
    def repl(m):
        name = m.group(1)
        n = len(name)
        if n % 2 == 0:
            half = name[: n // 2]
            if name == half + half and operator_catalog.is_operator(half):
                return half + '('
        return m.group(0)
    return _OP_CALL_RX.sub(repl, code)


def _add_missing_lookback(code: str, window: int) -> str:
    result = code
    # 끝에서 앞으로 처리해 삽입 후 인덱스가 어긋나지 않게 한다.
    for m in reversed(list(_OP_CALL_RX.finditer(code))):
        if not operator_catalog.needs_lookback(m.group(1)):
            continue
        open_idx = m.end() - 1            # '(' 위치
        depth = 0
        in_str = False
        close_idx = -1
        has_top_comma = False
        j = open_idx
        while j < len(result):
            ch = result[j]
            if ch == '"':
                in_str = not in_str
            elif not in_str:
                if ch == '(':
                    depth += 1
                elif ch == ')':
                    depth -= 1
                    if depth == 0:
                        close_idx = j
                        break
                elif ch == ',' and depth == 1:
                    has_top_comma = True
            j += 1
        if close_idx == -1 or has_top_comma:
            continue
        if not result[open_idx + 1:close_idx].strip():
            continue                       # op() 빈 인자는 미수정
        result = result[:close_idx] + f',{window}' + result[close_idx:]
    return result


def repair(code: str, *, delay) -> tuple[str, list[str]]:
    """(repaired_code, applied_fix_labels). 안전 수정만, 멱등."""
    if not isinstance(code, str) or not code.strip():
        return (code, [])
    applied: list[str] = []
    out = code

    s = _strip_region_prefix(out)
    if s != out:
        applied.append('region_prefix'); out = s
    s = _collapse_doubled_ops(out)
    if s != out:
        applied.append('doubled_op'); out = s
    s = _LEADING_OP_RX.sub('', out)
    if s != out:
        applied.append('leading_op'); out = s
    window = LOOKBACK_DELAY0 if str(delay) == '0' else LOOKBACK_DELAY1
    s = _add_missing_lookback(out, window)
    if s != out:
        applied.append('missing_lookback'); out = s

    return (out, applied)
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest tests/test_alpha_repair.py -v`
Expected: PASS (9 passed)

- [ ] **Step 5: 검증(체크포인트)** — PASS 확인.

---

## Task 4: db 마이그레이션 v1→v2 (settings 컬럼 + fp + self_corr + index)

**Files:**
- Modify: `server/db.py:44` (`_SCHEMA_VERSION`), `server/db.py:287-317` (alphas 마이그레이션 블록)
- Test: `tests/test_db_migration_v2.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_db_migration_v2.py`:
```python
import importlib
import sqlite3


def test_v2_columns_present(tmp_path, monkeypatch):
    monkeypatch.setenv('HYFE_IQC_DATA_DIR', str(tmp_path))  # DB 격리
    import server.db as db
    importlib.reload(db)
    db.init()
    con = sqlite3.connect(db.DB_PATH)
    cols = {r[1] for r in con.execute('PRAGMA table_info(alphas)').fetchall()}
    con.close()
    for c in ('region', 'universe', 'delay', 'neutralization', 'decay',
              'truncation', 'settings_fp', 'self_corr'):
        assert c in cols, c
    assert db._SCHEMA_VERSION >= 2


def test_migration_idempotent(tmp_path, monkeypatch):
    monkeypatch.setenv('HYFE_IQC_DATA_DIR', str(tmp_path))
    import server.db as db
    importlib.reload(db)
    db.init()
    db._INITIALIZED = False   # 재초기화 강제
    db.init()                 # 두 번째 init 은 예외 없이 통과해야 함
    con = sqlite3.connect(db.DB_PATH)
    ver = con.execute('PRAGMA user_version').fetchone()[0]
    con.close()
    assert ver == db._SCHEMA_VERSION
```

> **주의:** `HYFE_IQC_DATA_DIR` 로 DB 경로를 격리하는 부분은 db.py 의 실제 DB 경로 변수명에 맞춰 조정한다. db.py 상단에서 `DB_PATH`/데이터 디렉터리 결정 로직을 먼저 읽고(예: `grep -n "DB_PATH\|DATA_DIR\|\.db" server/db.py`), 테스트의 monkeypatch 대상을 그 변수에 맞게 수정할 것.

- [ ] **Step 2: 실패 확인**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest tests/test_db_migration_v2.py -v`
Expected: FAIL — `settings_fp` 등 컬럼 없음 / `_SCHEMA_VERSION < 2`

- [ ] **Step 3: 구현 — `_SCHEMA_VERSION` 올리기**

`server/db.py:44` 변경:
```python
_SCHEMA_VERSION = 2
```

- [ ] **Step 4: 구현 — alphas 신규 컬럼 ALTER 추가**

`server/db.py` 의 alphas 마이그레이션 블록에서, `phase` 컬럼 추가(308-313) **직후, 314행의 `CREATE INDEX idx_alphas_submitted` 앞**에 삽입:
```python
                # Phase 0: settings 타입 컬럼 + 캐시 fingerprint + self_corr (전부 nullable;
                # 기존 행은 universe/neut 미저장이라 NULL 유지가 정확 — 백필 없음).
                for _c, _decl in (
                    ('region', 'TEXT'), ('universe', 'TEXT'), ('delay', 'INTEGER'),
                    ('neutralization', 'TEXT'), ('decay', 'INTEGER'),
                    ('truncation', 'REAL'), ('settings_fp', 'TEXT'), ('self_corr', 'REAL'),
                ):
                    if _c not in alpha_cols:
                        conn.execute(f'ALTER TABLE alphas ADD COLUMN {_c} {_decl}')
                conn.execute(
                    'CREATE INDEX IF NOT EXISTS idx_alphas_hash_fp '
                    'ON alphas(code_hash, settings_fp)'
                )
```

- [ ] **Step 5: 통과 확인**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest tests/test_db_migration_v2.py -v`
Expected: PASS (2 passed)

- [ ] **Step 6: 검증(체크포인트)** — 기존 db 테스트도 깨지지 않는지: `python3.11 -m pytest tests/ -k db -v`.

---

## Task 5: db.insert_alpha (settings 컬럼 채움) + lookup_alpha_by_hash (settings_fp)

**Files:**
- Modify: `server/db.py:775-825` (insert_alpha), `server/db.py:828-845` (lookup_alpha_by_hash)
- Test: `tests/test_settings_cache.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_settings_cache.py`:
```python
import importlib


def _fresh_db(tmp_path, monkeypatch):
    monkeypatch.setenv('HYFE_IQC_DATA_DIR', str(tmp_path))  # Task4 와 동일 변수명으로 조정
    import server.db as db
    importlib.reload(db)
    db.init()
    return db


def test_cache_hit_same_settings(tmp_path, monkeypatch):
    db = _fresh_db(tmp_path, monkeypatch)
    uid = db.create_or_get_user_for_test() if hasattr(db, 'create_or_get_user_for_test') else 1
    code = 'rank(close)'
    alpha = {'idx': 1, 'code': code, 'pass_count': 7,
             'settings': {'universe': 'TOP500'}, 'delay': '1'}
    rid = db.start_round(uid, 1) if hasattr(db, 'start_round') else 1
    db.insert_alpha(uid, rid, 1, alpha)
    from server import settings_fp as sf
    fp = sf.settings_fingerprint(sf.effective_settings({'universe': 'TOP500'}, '1'))
    hit = db.lookup_alpha_by_hash(uid, db.code_hash(code), fp)
    assert hit is not None and int(hit['pass_count']) == 7


def test_cache_miss_different_settings(tmp_path, monkeypatch):
    db = _fresh_db(tmp_path, monkeypatch)
    code = 'rank(close)'
    alpha = {'idx': 1, 'code': code, 'pass_count': 7,
             'settings': {'universe': 'TOP500'}, 'delay': '1'}
    rid = db.start_round(1, 1) if hasattr(db, 'start_round') else 1
    db.insert_alpha(1, rid, 1, alpha)
    from server import settings_fp as sf
    other_fp = sf.settings_fingerprint(sf.effective_settings({'universe': 'TOP3000'}, '1'))
    assert db.lookup_alpha_by_hash(1, db.code_hash(code), other_fp) is None


def test_legacy_row_no_fp_is_miss(tmp_path, monkeypatch):
    db = _fresh_db(tmp_path, monkeypatch)
    code = 'rank(open)'
    # settings 없이 insert → settings_fp NULL (legacy 모사)
    rid = db.start_round(1, 1) if hasattr(db, 'start_round') else 1
    db.insert_alpha(1, rid, 1, {'idx': 1, 'code': code, 'pass_count': 3})
    from server import settings_fp as sf
    fp = sf.settings_fingerprint(sf.effective_settings({}, '1'))
    assert db.lookup_alpha_by_hash(1, db.code_hash(code), fp) is None
    # fp 미지정(legacy 경로) 조회는 여전히 동작
    assert db.lookup_alpha_by_hash(1, db.code_hash(code)) is not None
```

> **주의:** `start_round`/사용자 생성 헬퍼의 실제 함수명·시그니처는 db.py 를 읽어 맞춘다(`grep -n "def start_round\|def create_user\|def insert_round\|def add_user" server/db.py`). round_id FK 제약 때문에 insert_alpha 전에 유효한 round_id 가 필요하면 해당 헬퍼로 라운드를 먼저 만든다.

- [ ] **Step 2: 실패 확인**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest tests/test_settings_cache.py -v`
Expected: FAIL — `lookup_alpha_by_hash() takes 2 positional args` / settings 컬럼 미기록.

- [ ] **Step 3: 구현 — import 추가**

`server/db.py` 상단 import 영역에:
```python
from . import settings_fp as _settings_fp
```

- [ ] **Step 4: 구현 — insert_alpha 가 settings 컬럼/fp 기록**

`server/db.py:798-825` 의 INSERT 를 교체. 기존 컬럼 목록 끝에 신규 8개 컬럼과 값을 추가:
```python
    eff = _settings_fp.effective_settings(alpha.get('settings') or {},
                                          alpha.get('delay', ''))
    fp = _settings_fp.settings_fingerprint(eff)
    _sc = (alpha.get('metrics') or {}).get('self_corr')
    with _DB_LOCK, _connect() as conn:
        conn.execute(
            'INSERT INTO alphas (user_id, round_id, round_num, idx, code, code_hash, desc, '
            'pass_count, pass_items, fail_count, fail_items, error_text, metrics, mode, '
            'cached, submitted, submit_status, error_count, pending_count, phase, ts, '
            'region, universe, delay, neutralization, decay, truncation, settings_fp, self_corr) '
            'VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)',
            (
                user_id, round_id, round_num, int(alpha.get('idx') or 0),
                code, code_hash(code), alpha.get('desc', ''),
                pass_count,
                json.dumps(pass_items, ensure_ascii=False),
                fail_count,
                json.dumps(fail_items, ensure_ascii=False),
                alpha.get('error_text', ''),
                json.dumps(dict(alpha.get('metrics') or {}), ensure_ascii=False),
                alpha.get('mode', ''),
                1 if alpha.get('cached') else 0,
                1 if effectively_submitted(alpha.get('submitted'),
                                           str(alpha.get('submit_status') or '')) else 0,
                str(alpha.get('submit_status') or ''),
                error_count,
                pending_count,
                int(alpha.get('phase') or 0),
                time.time(),
                eff.get('region'), eff.get('universe'),
                int(eff['delay']) if str(eff.get('delay', '')).lstrip('-').isdigit() else None,
                eff.get('neutralization'),
                int(float(eff['decay'])) if eff.get('decay') not in (None, '') else None,
                float(eff['truncation']) if eff.get('truncation') not in (None, '') else None,
                fp,
                float(_sc) if isinstance(_sc, (int, float)) else None,
            ),
        )
```
> 비고: alpha 에 `settings`/`delay` 가 없으면 `effective_settings` 가 전부 기본값으로 채워 fp 를 만든다. legacy 호출(테스트 `test_legacy_row_no_fp_is_miss`)처럼 settings/delay 가 전혀 없는 경우에도 fp 는 "기본값" fp 가 되어버리므로, **legacy=NULL 의도를 지키려면**: `settings` 키가 alpha 에 아예 없으면 fp/컬럼을 NULL 로 둔다. 아래로 가드:
```python
    if 'settings' in alpha or 'delay' in alpha:
        eff = _settings_fp.effective_settings(alpha.get('settings') or {}, alpha.get('delay', ''))
        fp = _settings_fp.settings_fingerprint(eff)
    else:
        eff, fp = {}, None
```
(이 가드를 위 INSERT 앞에 두고, `eff.get(...)` 들은 `eff` 가 빈 dict 일 때 None 을 돌려주므로 그대로 안전.)

- [ ] **Step 5: 구현 — lookup_alpha_by_hash 에 settings_fp 인자**

`server/db.py:828-845` 교체:
```python
def lookup_alpha_by_hash(user_id: int, h: str,
                         settings_fp: str | None = None) -> dict[str, Any] | None:
    """동일 알파 코드의 가장 최근 시뮬 결과 — 사용자 전체에서 검색 (cross-user).

    settings_fp 가 주어지면 같은 settings 로 시뮬된 행만 매칭한다 (정확성). None 이면
    기존 동작(코드만). user_id 는 호환을 위해 받지만 필터에 쓰지 않는다.
    cross-user 공유는 의도된 효율 설계 — 단, WQB 티어별 데이터 접근 차이로 동일
    code+settings 가 사용자별 다른 결과를 낼 수 있다(알려진 한계).
    """
    if not h:
        return None
    init()
    with _DB_LOCK, _connect() as conn:
        if settings_fp is not None:
            row = conn.execute(
                'SELECT * FROM alphas WHERE code_hash=? AND settings_fp=? '
                'ORDER BY id DESC LIMIT 1',
                (h, settings_fp),
            ).fetchone()
        else:
            row = conn.execute(
                'SELECT * FROM alphas WHERE code_hash=? ORDER BY id DESC LIMIT 1',
                (h,),
            ).fetchone()
    return _alpha_view(row) if row else None
```

- [ ] **Step 6: 통과 확인**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest tests/test_settings_cache.py -v`
Expected: PASS (3 passed)

- [ ] **Step 7: 검증(체크포인트)** — `python3.11 -m pytest tests/ -k "db or cache" -v` 무회귀 확인.

---

## Task 6: result_cache.lookup 에 settings_fp 전달

**Files:**
- Modify: `server/result_cache.py:13-14`

- [ ] **Step 1: 구현**

`server/result_cache.py:13-14` 교체:
```python
def lookup(user_id: int, code: str, settings_fp: str | None = None) -> dict[str, Any] | None:
    return _db.lookup_alpha_by_hash(user_id, _db.code_hash(code), settings_fp)
```

- [ ] **Step 2: 검증** — 임포트 sanity:

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -c "from server import result_cache; print('ok')"`
Expected: `ok`

---

## Task 7: worker 통합 — 캐시 조회 fp 매칭 + insert 시 settings/delay 전달

**Files:**
- Modify: `server/worker.py:316-331` (캐시 분리 루프), `server/worker.py:486-505` (alpha_entry)

- [ ] **Step 1: 구현 — settings_by_idx 맵 + fp 매칭**

`server/worker.py` 상단 import 에 추가:
```python
from . import settings_fp as _settings_fp
```

`server/worker.py:314-331` 의 캐시 분리 루프를 교체:
```python
            # 캐시 hit 분리 (settings-aware 키: code_hash + settings_fingerprint).
            cached_results: list[dict] = []
            to_simulate: list[dict] = []
            seen: set[str] = set()
            settings_by_idx: dict[int, dict] = {
                int(s['idx']): (s.get('settings') or {}) for s in strategies
            }
            for s in strategies:
                eff = _settings_fp.effective_settings(s.get('settings') or {}, forced_delay)
                fp = _settings_fp.settings_fingerprint(eff)
                h = _db.code_hash(s['code'])
                key = f'{h}:{fp}'
                if key in seen:
                    continue
                seen.add(key)
                cached = result_cache.lookup(self.user_id, s['code'], fp)
                if cached:
                    cached_results.append(result_cache.materialize(s, cached, round_num))
                else:
                    to_simulate.append(s)
            cache_hit_total = len(cached_results)
```
> 기존 `metrics._delay` 동등 비교 블록(325-326)은 **삭제** — delay 가 fp 에 포함되어 불필요. dedup 키도 code-only(`h`)에서 `h:fp` 로 바꿔 같은 코드라도 settings 다르면 둘 다 시뮬되게 한다.

- [ ] **Step 2: 구현 — alpha_entry 에 settings/delay 실어 보내기**

`server/worker.py:486-503` 의 `alpha_entry` dict 에 두 키 추가 (마지막 `'phase': phase,` 다음):
```python
                    'phase': phase,
                    'settings': settings_by_idx.get(int(r['idx']), {}),
                    'delay': forced_delay,
```
> 캐시 재사용 행도 fp 가 매칭됐으므로 이번 라운드 settings 가 곧 그 행의 settings 다 → settings_by_idx 사용이 정확.

- [ ] **Step 3: 검증** — 임포트/문법 sanity:

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -c "import ast; ast.parse(open('server/worker.py').read()); print('ok')"`
Expected: `ok`

---

## Task 8: 연산자 카탈로그 소비 — alpha_similarity + operator_preference_stats

**Files:**
- Modify: `server/alpha_similarity.py:17-40`, `server/db.py:971-994`
- Test: `tests/test_alpha_similarity_catalog.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_alpha_similarity_catalog.py`:
```python
from server import alpha_similarity as sim


def test_known_op_not_counted_as_field():
    # ts_backfill 같이 구 _KNOWN_OPS 에 없던 연산자가 카탈로그에 있으면 field 로 안 샌다.
    # (카탈로그가 인식하는 op 은 field 집합에서 제외)
    ops = sim.extract_operators('ts_mean(close,5)')
    assert 'ts_mean' in ops
    flds = sim.extract_fields('ts_mean(close,5)')
    assert 'ts_mean' not in flds
    assert 'close' in flds
```

- [ ] **Step 2: 실패/통과 베이스라인 확인**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest tests/test_alpha_similarity_catalog.py -v`
(기존 구현으로도 통과할 수 있으나, 카탈로그 전환 후에도 유지되는지 회귀 가드로 둔다.)

- [ ] **Step 3: 구현 — alpha_similarity 가 카탈로그 사용**

`server/alpha_similarity.py:17-26` 의 `_KNOWN_OPS = frozenset({...})` 정의를 삭제하고, 파일 상단 import 에 추가:
```python
from . import operator_catalog
```
그리고 `extract_operators`(37-40), `extract_fields`(43-54) 의 `_KNOWN_OPS` 참조를 교체:
```python
def extract_operators(code: str) -> set[str]:
    return {m.group(1).lower() for m in _OP_PAT.finditer(code or '')
            if operator_catalog.is_operator(m.group(1))}


def extract_fields(code: str) -> set[str]:
    fields: set[str] = set()
    for m in _TOKEN_PAT.finditer(code or ''):
        tok = m.group(1).lower()
        if operator_catalog.is_operator(tok) or len(tok) < 4:
            continue
        fields.add(tok)
    return fields
```

- [ ] **Step 4: 구현 — operator_preference_stats 가 카탈로그 사용**

`server/db.py:971-980` 의 로컬 `KNOWN_OPS = {...}` 정의를 삭제하고, 같은 함수 내 두 군데 참조(988, 993)를 교체:
```python
        ops_in_code = {m.group(1).lower() for m in op_pat.finditer(code)
                       if _operator_catalog.is_operator(m.group(1))}
        for op in ops_in_code:
            op_used.setdefault(op, []).append(pc)
        for m in tok_pat.finditer(code):
            tok = m.group(1).lower()
            if _operator_catalog.is_operator(tok) or len(tok) < 4:
                continue
            field_used.setdefault(tok, []).append(pc)
```
`server/db.py` 상단 import 에:
```python
from . import operator_catalog as _operator_catalog
```

- [ ] **Step 5: 통과 확인**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest tests/test_alpha_similarity_catalog.py tests/test_operator_catalog.py -v`
Expected: PASS

- [ ] **Step 6: 검증(체크포인트)** — `python3.11 -c "from server import db, alpha_similarity; print('ok')"` → `ok`.

---

## Task 9: gemini_strategist — _filter_by_lint 에 자동수정 통합

**Files:**
- Modify: `server/gemini_strategist.py:653-671` (_filter_by_lint), 호출부 `:791`, `:971`

- [ ] **Step 1: 구현 — _filter_by_lint 시그니처 + repair 호출**

`server/gemini_strategist.py:653-671` 교체:
```python
def _filter_by_lint(strategies: list[dict], log_fn: Callable | None = None,
                    forced_delay=None) -> list[dict]:
    try:
        from . import alpha_lint
    except Exception:
        return strategies
    from . import alpha_repair
    clean: list[dict] = []
    rejected: list[tuple[int, str, list[str]]] = []
    for s in strategies:
        fixed, applied = alpha_repair.repair(s['code'], delay=forced_delay)
        if applied:
            s['code'] = fixed
            if log_fn:
                log_fn(f"  #{s['idx']} 자동수정({', '.join(applied)}): {s['code'][:100]}")
        bads = _alpha_violations(s['code'])
        if not bads:
            bads = alpha_lint.validate_alpha(s['code'])
        if bads:
            rejected.append((s['idx'], s['code'], bads))
        else:
            clean.append(s)
    if rejected and log_fn:
        for idx, code, bads in rejected:
            log_fn(f'⚠ #{idx} lint 거부 ({"; ".join(bads)[:200]}): {code[:120]}')
    return clean
```

- [ ] **Step 2: 구현 — 호출부 2곳에 forced_delay 전달**

`server/gemini_strategist.py:791`:
```python
                clean = _filter_by_lint(strategies, log_fn=log_fn, forced_delay=forced_delay)
```
`server/gemini_strategist.py:971` (generate_focused_strategies 내부, 동일하게):
```python
                clean = _filter_by_lint(strategies, log_fn=log_fn, forced_delay=forced_delay)
```

- [ ] **Step 3: 실패하는 통합 테스트 작성**

`tests/test_filter_repair.py`:
```python
from server import gemini_strategist as gs


def test_filter_autofix_region_prefix_then_passes():
    strategies = [{'idx': 1, 'code': 'rank(USA.close)', 'desc': '', 'settings': {}}]
    out = gs._filter_by_lint(strategies, log_fn=None, forced_delay='1')
    assert len(out) == 1
    assert out[0]['code'] == 'rank(close)'


def test_filter_autofix_missing_lookback_delay0():
    strategies = [{'idx': 1, 'code': 'ts_mean(close)', 'desc': '', 'settings': {}}]
    out = gs._filter_by_lint(strategies, log_fn=None, forced_delay='0')
    assert out and out[0]['code'] == 'ts_mean(close,22)'
```

- [ ] **Step 4: 통과 확인**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest tests/test_filter_repair.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 검증** — `python3.11 -c "from server import gemini_strategist; print('ok')"` → `ok`.

---

## Task 10: 전체 회귀 + 반영

- [ ] **Step 1: 전체 테스트**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -m pytest -v`
Expected: 전 항목 PASS (기존 테스트 무회귀 + 신규 모두 PASS)

- [ ] **Step 2: 임포트 스모크**

Run: `cd /home/opc/projects/HYFE_IQC && python3.11 -c "from server import app, worker, db, gemini_strategist, result_cache, settings_fp, operator_catalog, alpha_repair, alpha_similarity; print('all import ok')"`
Expected: `all import ok`

- [ ] **Step 3: 반영 (사장 승인 후)**

```bash
sudo systemctl restart hyfe-iqc.service
sudo systemctl status hyfe-iqc.service   # port 8088, active 확인
```
> 첫 1~2 라운드는 새 fp 키로 캐시가 비어 재시뮬 증가가 정상. 로그에서 `자동수정(...)` 라인과 캐시 hit 율 회복을 관찰.

- [ ] **Step 4: 검증** — 라운드 1회 무에러 완주 + DB 신규 컬럼 채워짐 확인:
```bash
python3.11 -c "import sqlite3,glob; p=glob.glob('data/*.db')[0]; c=sqlite3.connect(p); print(c.execute('SELECT code, settings_fp, universe, delay FROM alphas WHERE settings_fp IS NOT NULL ORDER BY id DESC LIMIT 3').fetchall())"
```
Expected: 최근 알파에 settings_fp/universe/delay 채워진 행 출력.

---

## Self-Review 결과 (작성자 점검)

- **스펙 커버리지:** §3 마이그레이션→Task4, §4 캐시키→Task1/5/6/7, §5 자동수정→Task3/9, §6 카탈로그→Task2/8, §7 테스트→각 Task. 누락 없음.
- **타입 일관성:** `effective_settings(partial, forced_delay)`·`settings_fingerprint(eff)`·`is_operator`/`needs_lookback`·`repair(code,*,delay)->(str,list)`·`lookup_alpha_by_hash(user_id,h,settings_fp=None)`·`result_cache.lookup(user_id,code,settings_fp=None)` — Task 간 시그니처 일치.
- **알려진 조정 포인트(placeholder 아님, 실코드 확인 필요):** Task4/5 의 테스트 DB 격리 변수명(`HYFE_IQC_DATA_DIR`/`DB_PATH`)과 라운드/유저 생성 헬퍼명은 db.py 실제 정의에 맞춰 조정(각 Task 의 주의 박스에 grep 명령 명시).
- **정책 준수:** 자동수정은 4개 안전 케이스만·멱등; 카탈로그는 시드 합집합으로 무회귀; denylist 정적 유지; 커밋은 사장 승인 시.
